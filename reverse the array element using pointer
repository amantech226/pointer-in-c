#include <stdio.h>

int main()
{
    int a[5],i=0,len,j=0;
    int *p;
    len=sizeof(a)/sizeof(a[0]);
    
    
   for(p=a;p<&a[len];p++)
   {
       scanf("%d",p);
   }
   
   for(p=&a[len-1];p>=a;p--)
   {
      printf("\n%d",*p);
   }

    return 0;
}
